#pragma once
#include "DxLib.h"

namespace Color
{
	const int WHITE = GetColor(255, 255, 255);
	const int RED = GetColor(255, 0, 0);

	// テキストカラー
	const int TEXT = WHITE;

	// Playerで使用
	const int LINE = RED;
}
